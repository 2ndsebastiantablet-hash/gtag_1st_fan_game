{
  "name": "feeble-multiplayer-worker",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "node -e \"console.log('Static frontend ready for Cloudflare Pages.')\"",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
